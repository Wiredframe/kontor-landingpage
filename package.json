{
  "name": "kontor-landingpage",
  "version": "1.0.0",
  "private": true,
  "description": "Statische Landingpage für Kontor. Tailwind wird lokal vorkompiliert (keine Play-CDN), Schrift self-hosted.",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o assets/tailwind.css --minify",
    "watch:css": "tailwindcss -i src/input.css -o assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
